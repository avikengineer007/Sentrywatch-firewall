# Sentrywatch-firewall
Sentrywatch v2 — CLI IDS/IPS detecting brute-force, port scans, priv-esc, anomalous outbound traffic, and suspicious processes from system logs. Resolves source IPs, tracks reputation, scores severity via Claude (advisory-only). Blocking stays deterministic, TTL-limited, allowlist-gated. Terminal UI + notifications.
